import Hero from "./Hero";
import NavBar from "./Nav";

export default function Header() {
    return (
        <header className="overflow-visible h-auto relative">
            {/* <NavBar /> */}
            <Hero />
        </header>
    );
}
